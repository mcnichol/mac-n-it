#!/usr/bin/env bash

export SCRIPT_FOLDER=${cwd}/scripts
export CONFIG_FOLDER=${cwd}/config
export PLUGIN_FOLDER=${cwd}/plugins
export RESOURCE_FOLDER=${cwd}/res
export RC_FOLDER=${RESOURCE_FOLDER}/rc
export TOP_DIR=${cwd}